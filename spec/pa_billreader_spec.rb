require "spec_helper"

describe PaBillreader do
  it "has a version number" do
    expect(PaBillreader::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
